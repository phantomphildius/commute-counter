<h2>You've saved <%= @savings_calculator.savings.format %></h2>

<%= link_to 'Add new commute gear', new_commute_gear_path %>

<ul>
<% @activities.each do |run| %>
  <li>
    <%= run.id %>
    <%= run.name %>
  </li>
<% end %>
</ul>
